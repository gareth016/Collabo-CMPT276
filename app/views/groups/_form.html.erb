<%= form_for(@group) do | f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_name %>
    <%= f.text_field :group_name %>
  </div>

  <div class="field">
    <%= f.label :group_info %>
    <%= f.text_area :group_info %>
  </div>

  <div class="field">
    <%= f.label :user_ids %>
    <%= f.number_field :user_ids %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
